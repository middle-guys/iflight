<nav class="navbar navbar-full navbar-light">
  <%= link_to root_path, class: "navbar-brand" do %>
    <div class='logo iflight'></div>
    <div class='logo iflight-text'></div>
  <% end %>
  <div class="pull-xs-right" style="margin-top: 10px;">
    <div class="btn-group">
      <button class="btn btn-secondary dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <% if user_signed_in? %>
        <%= current_user.name %>
      <% else %>
        User name
      <% end %>
      </button>
      <div class="dropdown-menu dropdown-menu-right">
        <% if user_signed_in? %>
          <li>
          <%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "dropdown-item") %>    
          </li>
          <% else %>
            <li>
            <%= link_to('Login', new_user_session_path, class: "dropdown-item")  %>  
            </li>
            <li><%= link_to('Register', new_user_registration_path, class: "dropdown-item")  %>
            </li>
          <% end %>
      </div>
    </div>
  </div>
</nav>